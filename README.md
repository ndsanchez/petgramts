# Petgram with Typescript

# Installation

Install the following dependencies

node

On the root folder run
```
$ npm install
```

for running the dev server
```
$ npm run dev
```

for running the tests
```
$ npm run test
```

for building the app
```
$ npm run build
```
